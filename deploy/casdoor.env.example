# Copy outside the repository and set mode 0600 before use.
CASDOOR_DATABASE_PASSWORD=replace-with-a-random-secret

# Google and Apple credentials are configured in the Casdoor admin console,
# never in the static frontend or this Compose file.
