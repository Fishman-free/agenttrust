\section{Introduction}
\label{sec:intro}

Large language models and tool-use systems can plan, call external services, and initiate
economically consequential actions. Open agent-to-agent commerce therefore raises familiar but
important questions about identity, escrow, evidence, dispute resolution, and liability
\cite{a2a-finance,agent-rep}. Cheap pseudonyms complicate accountability, while opaque execution
makes the merits of many disputes difficult to verify automatically.

\paragraph{Problem and scope.}
We study a narrow systems question: what can be learned from coupling principal-bound agent
identifiers, collateralized escrow, a simple staked vote, and outcome counters in one prototype? The
prototype does not establish the off-chain truth of a dispute, legal identity, or legal liability. It
also does not solve secure evidence collection, voter selection, bribery, privacy, oracle failure, or
contract governance. ``Principal-bound'' below means only that a token and stored metadata are
associated with an on-chain address; it is not proof that the address corresponds to a unique human
or legally responsible entity.

\paragraph{Prototype architecture.}
The identity--trust--accountability (ITA) decomposition in Section~\ref{sec:ita} maps to four legacy
Solidity contracts: \textsf{AgentRegistry}, \textsf{GuaranteeEscrow},
\textsf{SchellingVoting}, and \textsf{ReputationHub}. The intended research design is a feedback
cycle in which outcomes update reputation and reputation informs later guarantee quotes. In the
inspected legacy snapshot, however, the contracts record outcomes and accept a caller-supplied
premium; they do not compute or enforce a reputation-dependent premium. The closed loop is thus a
proposed policy layer whose integration remains to be verified in the parallel implementation.

\paragraph{Version statement.}
Implementation descriptions in this paper refer to the repository state inspected on August 10,
2026, before the concurrent contract and simulation revisions were frozen. They are descriptive, not
a claim about the final code. Historical repository material reports four contracts and 38 Foundry
tests, but this paper does not treat that count as an audit or as evidence about untested behaviors.
No new implementation result from the in-progress revision is reported here.

\paragraph{Contributions.}
\begin{itemize}[leftmargin=2em,itemsep=2pt]
  \item A research-prototype architecture connecting identity records, collateralized escrow,
  staked dispute voting, and outcome counters.
  \item A code-scoped description of the legacy $65\%$ rule, including quorum, void-case,
  abstention-refund, capital-at-risk, and integer-dust behavior.
  \item Conditional accounting and threshold propositions with explicit hypotheses, replacing
  unsupported claims of strict Bayesian--Nash truthfulness, harmless minorities, unprofitable Sybils,
  and exact conservation.
  \item An exploratory evaluation report that distinguishes simulation observations from contract
  verification and documents what must be rerun after a release is frozen.
\end{itemize}

\paragraph{Organization.}
Section~\ref{sec:related-work} reviews related systems. Section~\ref{sec:ita} presents the ITA
architecture and threat boundaries. Section~\ref{sec:mech} states the mechanism and limited analytical
results. Section~\ref{sec:impl} describes the legacy implementation and exploratory evaluation.
Section~\ref{sec:conclusion} concludes with limitations and a validation plan.
